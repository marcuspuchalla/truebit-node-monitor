# TrueBit Monitor Configuration

# Domain for HTTPS (used with docker-compose.https.yml)
DOMAIN=monitor.yourdomain.com

# Email for Let's Encrypt certificate notifications
ACME_EMAIL=your-email@example.com

# Federation NATS server (default: public federation server)
FEDERATION_NATS_URL=wss://f.tru.watch:9086

# CORS: Comma-separated list of allowed origins (use * for all)
# Example: https://monitor.yourdomain.com,http://localhost:8090
ALLOWED_ORIGINS=*

# WebSocket authentication (optional, set to 'true' to require auth)
WS_AUTH_REQUIRED=false
# WS_AUTH_TOKEN=your-secure-token-here

# Docker container to monitor
CONTAINER_NAME=runner-node

# Database path
DB_PATH=/app/data/truebit-monitor.db

# Log retention in days
LOG_RETENTION_DAYS=30
