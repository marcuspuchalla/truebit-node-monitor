# TrueBit Node Monitor - Environment Configuration
# Copy this file to .env and modify as needed

# =============================================================================
# HTTPS DEPLOYMENT (for docker-compose.https.yml)
# =============================================================================

# Your domain name
DOMAIN=monitor.yourdomain.com

# Email for Let's Encrypt certificate notifications
ACME_EMAIL=your-email@example.com

# =============================================================================
# AUTHENTICATION
# =============================================================================

# Password for accessing task data in the dashboard
# If not set, a random password is generated at startup
# Find it in logs: docker logs truebit-node-monitor | grep "key"
# TASK_DATA_PASSWORD=your-secure-password-here

# API key for programmatic access (optional)
# If set, all API requests require: Authorization: Bearer <API_KEY>
# API_KEY=your-api-key-here

# =============================================================================
# TRUEBIT NODE
# =============================================================================

# Name of the TrueBit node container to monitor
CONTAINER_NAME=runner-node

# =============================================================================
# FEDERATION
# =============================================================================

# Public federation server for anonymized statistics sharing
FEDERATION_NATS_URL=wss://f.tru.watch

# =============================================================================
# OPTIONAL SETTINGS
# =============================================================================

# Database path inside container
DB_PATH=/app/data/truebit-monitor.db

# Log retention in days
LOG_RETENTION_DAYS=30

# Server port
PORT=8090

# CORS allowed origins (comma-separated, default: * allows all)
# For production, restrict to your domain:
# ALLOWED_ORIGINS=https://monitor.yourdomain.com

# Enable HTTPS security headers (set to true when behind HTTPS proxy)
HTTPS_ENABLED=false

# WebSocket authentication (optional)
WS_AUTH_REQUIRED=false
# WS_AUTH_TOKEN=your-secure-token-here
